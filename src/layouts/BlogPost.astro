---
import BaseHead from '../components/BaseHead.astro';
import Body from '../components/Body.astro';
import Content from '../components/Content.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';

export interface Props {
  content: {
    title: string;
    description: string;
    createdAt?: string;
    updatedAt?: string;
    heroImage?: string;
    tags: string[];
  };
}

const {
  content: { title, description, heroImage },
} = Astro.props;
---

<html lang="en">
  <head>
    <BaseHead title={title} description={description} image={heroImage} />
  </head>
  <Body>
    <Header />
    <main class="pt-[56px]">
      <Content className="pt-6">
        <div class="mb-8">
          {
            heroImage && (
              <img
                src={heroImage}
                alt=""
                class="border border-slate-300 dark:border-zinc-700 rounded-xl w-[720px] h-[360px]"
              />
            )
          }
        </div>
        <h1 class="text-3xl my-1 font-mplus text-center">{title}</h1>
        <!-- <div>{description}</div> -->
        <hr class="border-top border-zinc-400 my-4" />
        <slot />
        <!-- <span
          class="underline underline-offset-2 hover:text-orange-500 decoration-orange-500 text-2xl list-disc"
        ></span> -->
      </Content>
    </main>
    <Footer />
  </Body>
</html>
